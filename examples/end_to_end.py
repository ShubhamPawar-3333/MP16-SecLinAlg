"""Tiny end-to-end demo over p = 101 (SDD 15).

Must exist from week 1 and grow into the integration demo:
    1. build two small matrices over Field(101)
    2. multiply them in the clear
    3. secret-share the entries, run the private matrix product, reconstruct
    4. assert the reconstructed result equals the plaintext product
"""

def main() -> None:
    raise NotImplementedError("integration demo -- wire together as worklets land")


if __name__ == "__main__":
    main()
